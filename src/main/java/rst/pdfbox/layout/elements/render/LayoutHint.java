package rst.pdfbox.layout.elements.render;

import rst.pdfbox.layout.elements.Document;

/**
 * Each element in a document is
 * {@link Document#add(rst.pdfbox.layout.elements.Element, LayoutHint)
 * accompanied} by a hint, which gives some notes to the current layout
 * on how to layout the element.
 */
public interface LayoutHint {

}
